- name: Configure OS X default plist settings
  include_role: osx_defaults

- name: Install Homebrew packages
  include_role: homebrew

- name: Install Homebrew Cask applications
  include_role: homebrew_cask
